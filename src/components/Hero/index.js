import React from 'react';
import Card from '../UI/Card';
import './styles.css';

const Hero=(props)=>{
    return(
        <div>
            <Card>
                oussama ja
            </Card>
        </div>
    );
}

export default Hero;
